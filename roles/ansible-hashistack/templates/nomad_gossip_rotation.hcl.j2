vault {
  address      = "{{ global_vault_address }}"
  token        = "{{ nomad_gossip_token }}"
  unwrap_token = false
  renew_token  = true
}

template {
  source      = "/opt/nomad/templates/gossip.key.tpl"
  destination = "/opt/nomad/templates/gossip.key"
  perms       = 0700
  command     = "/opt/nomad_rotate_key.sh"
}

