vault {
  address      = "http://{{ inventory_hostname }}:8200"
  token        = "{{ root_token }}"
  unwrap_token = false
  renew_token  = false
}

template {
  source      = "/opt/nomad/templates/gossip.key.tpl"
  destination = "/opt/nomad/gossip/gossip.key"
  perms       = 0700
  command     = "/opt/nomad_rotate_key.sh"
}

